{
  "name": "vjbox",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-only audio-reactive VJ visualizer. Drop an audio file, get live visuals.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  }
}
